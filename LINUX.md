# Linux Command Cheatsheet
### File Zip/Unzip
Unzipping gz File 
```bash
gzip -d file.gz```
Unzipping gz File and keep original file 
```bash
gzip -dk file.gz```
Open a `.gz` file with `gunzip`
```bash
gunzip file.gz```


